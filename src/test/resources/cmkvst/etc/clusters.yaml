cluster: {
  host1: {
     hostId: 1,
     tcpBufferSize: 65536,
     hostname: localhost,
     port: 8080,
     timeoutMs: 1000,
  },
  host2: {
     hostId: 2,
     tcpBufferSize: 65536,
     hostname: localhost,
     port: 8081,
     timeoutMs: 1000,
  },
  host3: {
     hostId: 3,
     tcpBufferSize: 65536,
     hostname: localhost,
     port: 8082,
     timeoutMs: 1000,
  }
}
clusterTwo: {
  host1: {
     hostId: 1,
     tcpBufferSize: 65536,
     hostname: localhost,
     port: 8083,
     timeoutMs: 1000,
  },
  host2: {
     hostId: 2,
     tcpBufferSize: 65536,
     hostname: localhost,
     port: 8084,
     timeoutMs: 1000,
  },
  host3: {
     hostId: 3,
     tcpBufferSize: 65536,
     hostname: localhost,
     port: 8085,
     timeoutMs: 1000,
  }
}